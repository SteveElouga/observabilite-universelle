# --- Secrets & environnement (règle : jamais de secret dans Git — CONTEXT.md) ---
.env
*.env
!.env.example
observability/alertmanager/secrets/*
!observability/alertmanager/secrets/*.example

# --- Artefacts générés (à régénérer, pas à versionner à la main) ---
observability/prometheus/rules/rules-slo.yml

# --- Données & volumes locaux de la stack ---
observability/**/data/
observability/**/*.local.*

# --- Sorties de sauvegarde (durcissement) ---
observability/hardening/backups/

# --- OS / éditeurs ---
.DS_Store
Thumbs.db
.idea/
.vscode/
.obsidian/

# --- Python (instrumentation Django à venir) ---
__pycache__/
*.pyc
.venv/

# --- Node (frontend Angular / k6 à venir) ---
node_modules/
